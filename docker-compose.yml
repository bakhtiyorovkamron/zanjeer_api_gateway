version: '3'

services:
  app:
    build: .
    ports:
      - "9999:7777"
  migrate:
    container_name: migrate
    image: migrate/migrate:v4.15.2
    volumes:
      - ./migrations:/migrations
    entrypoint: migrate -path ./migrations -database "postgres://zanjeer:password@5.39.92.50:5432/zanjeer?sslmode=disable"
    command: up
    restart: on-failure





